# AGENTS.md

Go 1.26+ proxy server providing OpenAI/Gemini/Claude/Codex compatible APIs with OAuth and round-robin load balancing.

## Repository
- Fork origin: https://github.com/yuanwangz/CLIProxyAPI.git
- Upstream GitHub: https://github.com/router-for-me/CLIProxyAPI

## Fork Maintenance Notes
- This file is the canonical agent note for this repository. If a request says `AGENT.md`, update `AGENTS.md` unless the user explicitly asks for a separate file.
- This repository is a maintained fork of `router-for-me/CLIProxyAPI`. Keep the fork close to upstream and prefer upstream-compatible changes so future upstream merges stay small.
- Local backend path: `/Users/yuanwan/CLIProxyAPI`
- Related frontend path: `/Users/yuanwan/Cli-Proxy-API-Management-Center`
- Frontend fork origin: https://github.com/yuanwangz/Cli-Proxy-API-Management-Center
- Frontend upstream GitHub: https://github.com/router-for-me/Cli-Proxy-API-Management-Center
- After any major fork-specific behavior change, upstream merge, frontend/backend contract change, or release-process change, update this `AGENTS.md` in the same work session so future agents do not need the same context repeated.

### Upstream Compatibility Rules
- Treat upstream files as shared ownership. Prefer narrow, additive patches over broad rewrites, wholesale formatting, or moving code between packages.
- Keep fork-only behavior behind clearly named packages, helper functions, feature flags, config fields, or adapter layers where practical. Avoid interleaving large fork-specific blocks directly inside upstream hot paths.
- When a fork feature must hook into an upstream-owned file, keep the hook minimal and delegate the real logic to a fork-owned package. This reduces conflict surface during upstream sync.
- Current project adjustment principle: keep feature changes low-intrusion, incremental, and fork-friendly. Prefer existing management APIs, persistence packages, and frontend integration points before touching request execution, scheduler, conductor, or provider executor hot paths.
- Do not delete or "simplify away" fork-specific features when resolving upstream conflicts. First identify whether upstream removed a feature globally or only refactored the surrounding code.
- During upstream merges, preserve upstream improvements while restoring local fork features. For conflicts, compare both sides, identify the behavioral intent, then re-apply the fork feature in the smallest compatible shape.
- Avoid repository-wide format churn. Run `gofmt` only after Go changes, and keep frontend formatting scoped to touched files.
- Add or keep tests around fork-owned behavior so merge regressions are caught before deployment.

### Frontend Fork UX Principle
- Treat the existing fork frontend interaction model as an intentional product decision. Take upstream bug fixes, compatibility work, provider support, data-flow improvements, accessibility fixes, and small targeted usability improvements when they fit the existing UI.
- Do not replace a fork page merely because upstream performed a visual or structural redesign. Keep the fork layout when it is already more effective for daily operations, especially dense multi-credential views, bulk actions, filtering, and comparison workflows.
- For the quota page specifically, keep `/quota` on the fork's list-oriented `src/pages/QuotaPage` unless the user explicitly decides otherwise. The upstream card/timeline implementation may be mined for provider adapters or isolated behavior fixes, but it must not silently replace the fork page or discard Gemini CLI, persisted snapshots, token summaries, multi-credential selection, search/filtering, pagination, or batch refresh behavior.

### Recent Upstream Sync Notes
- 2026-08-19: completed the usage-statistics contract for request reasoning intensity. The fork now persists `reasoning_effort` in `internal/usagepersist` (including SQLite migration, JSONL import/export, event hashing, and `/v0/management/usage` detail output), and the frontend appends the value to recent-request model labels as `model (high)`. Legacy events without the field remain unchanged. Preserve this additive field during future backend/frontend merges.
- 2026-08-18: merged backend upstream `main` through `d3a5988f` and frontend upstream `main` through `73db424f`. Backend kept fork-owned Gemini CLI, usagepersist SQLite cooldown/quota persistence, archived credentials, image fallback, failure warmup, quota routing hints, and consecutive xAI 403 disable behavior while adopting upstream's current executor/translator/config/plugin/live-media/model updates. The fork's explicit unauthorized state remains durable and zero-cooldown; provider lifecycle/authentication errors that are not explicit 401 unauthorized continue to use model/credential cooldown and rotation. Frontend absorbed upstream's data-flow, provider, config, dashboard, workbench, accessibility, and compatibility improvements while retaining the fork auth-files behavior (archive and 401/403/429 filters, credential inspection, bulk field editing), Gemini CLI quota, hidden sponsor entry points, cooldown clearing, and failure-warmup editing. The upstream quota card/timeline redesign was reviewed but did not replace the fork's dense list-oriented `/quota` page because it is less suitable for multi-credential operations; the fork page keeps Gemini CLI, persisted snapshots, cycle-token summaries, search/filtering, pagination, selection, and batch refresh. Frontend quota merge resolution keeps Kimi reset instants/time units and xAI weekly/monthly billing periods atomic; no conflict markers remain. The frontend `ApiKeyStrengthMeter` also uses state for previous animation segments to satisfy the current React hooks lint rule without changing behavior.
- 2026-07-29: added fork-owned bulk field editing to the frontend auth-files page, modeled after `Mxucc/cpa-account-config-manager` while remaining compatible with the existing management API. Batch edits operate only on explicitly selected non-runtime auth files, require per-field opt-in plus confirmation, reuse `PATCH /auth-files/fields` with bounded frontend concurrency, preserve unmentioned headers, and keep failed targets selected for retry. Auth-file facets now flow top-down: provider selection defines the data domain for status-code/archive/result filters and their counts, while provider badge counts remain global so lower filters cannot feed back into the upper provider rail. Preserve this direction and the opt-in patch semantics during future auth-files page merges.
- 2026-07-27: rebuilt the frontend fork's credential inspection as evidence-based diagnosis instead of auth-state echo. Disabled and archived auth files are probed by stable `auth_index`; results separate provider health (`healthy` / `limited` / `reauth` / `review` / `error`) from persisted lifecycle state and expose confirmation-gated quick actions. Codex recovery requires usable long-term and 5-hour windows; incomplete long-window evidence stays review-only. HTTP 429 and exhausted quota windows are recoverable cooldown conditions and must never suggest or write `disabled=true`; backend runtime recovery remains driven by `Quota.Exceeded` / `NextRetryAfter`, while successful active-credential snapshots may clear stale quota cooldowns. Ambiguous 402/403/404 responses remain review-only unless provider response semantics are explicit.
- 2026-07-27: merged backend upstream `main` through `8423cce2` (`v7.2.102`) and frontend upstream through `21af576` (`v1.19.3`). Backend upstream performed broad responsibility-based splits of server/config/request-logging/pluginhost/provider executors/handlers/conductor/service files; fork hooks were migrated into the new files instead of retaining the old monoliths. Backend conflicts in `docker-compose.yml` (keep fork data volume + upstream plugin volume), `go.mod`/`go.sum` (keep fork SQLite dependencies + upstream Codex Live/tokenizer dependencies), management auth files (adopt upstream CRUD/fields/OAuth split while restoring archived/created-at/health/xAI-safe-identity/manual-refresh and fork Gemini CLI OAuth in `auth_files_gemini_cli.go`), server/config (restore usagepersist/Gemini CLI/failure-warmup routes and config on upstream split), pluginhost (keep legacy empty format → OpenAI compatibility), Antigravity (keep daily `loadCodeAssist` default), Codex HTTP/WebSocket executors (restore quota persistence and case-preserved headers on upstream split), usage helpers (combine fork empty additional-model event + suppression/Gemini CLI usage with upstream token accounting v2), handlers (keep bootstrap `auth_unavailable` normalization and archived websocket exclusion), conductor (restore archived selection, endpoint-scoped image 401 handling, durable unauthorized/403 disable, failure warmup, usagepersist cooldown hooks, operator cooldown clear, and manual refresh across upstream split), and service (restore Gemini CLI/archived/model cooldown hooks while keeping all upstream file/PostgreSQL cooldown stores hard-disabled). Adjusted upstream cooldown-store and filestore tests to assert the fork contracts. Backend adopted upstream Codex Live WebRTC/media relay, credential concurrency/Home in-flight accounting, derived session identity, Codex/Claude client model catalogs, local Claude token counting, normalized token accounting v2, PostgreSQL cooldown-store implementation (merged but disabled by fork), configurable xAI `x_search` injection, response/tool-name normalization, and Claude Opus 5 / Gemini 3.6 Flash model updates. Frontend conflicts in `MainLayout`/layout styles (adopt upstream compact rail tooltips while retaining fork branding/status footer), auth-files page/styles (retain fork dense table, archive + 401/403/429 filters and credential inspection while wiring upstream OAuth manual refresh/change events), and all locales (retain fork text plus upstream sidebar accessibility/count keys). Frontend also adopted upstream API error parsing, xAI paid OAuth quota fallback, Kimi surface styling/quota ordering, Claude API legacy URL compatibility, and removal of Home detection/log payload support; Gemini CLI OAuth/quota, Codex reset credits, usage TTFT/provider attribution, and hidden sponsor entry points remain intact.
- 2026-07-20: merged backend upstream `main` through `fde40c5a` (`v7.2.91`). Frontend upstream `main` had no new commits beyond previous `6a6a22a` (`v1.18.5`). Backend conflicts in `internal/runtime/executor/codex_websockets_executor.go` (keep fork `codex.rate_limits` → `codexquota.PersistFromEvent` while adopting upstream `response.output_item.done` collection / `patchCodexCompletedOutput` / reasoning-replay cache on `response.completed`), `internal/translator/common/bytes.go` (keep fork `WrapGeminiCLIResponse` and adopt upstream `gjson` for `SetRawArrayItems`), and `sdk/cliproxy/executor/types.go` (keep fork `SkipSelectedAuthResultMetadataKey` and adopt upstream `SelectedAuthIndexMetadataKey` / `SelectedAuthIndexCallbackMetadataKey`). Post-merge compile fix: fork `selected_auth_execute.go` now passes `*Auth` into upstream-shaped `publishSelectedAuthMetadata`. Verified intact after merge: hard-disabled `.cds` cooldown store in `configureCooldownStateStore`, consecutive status-disable hooks in `MarkResult`, Gemini CLI surfaces (`WrapGeminiCLIResponse`/`FormatGeminiCLI`/executor/auth packages), image-fallback `SkipSelectedAuthResult`, and `usagepersist` / `WithUsageSuppressed`. Backend also adopted upstream plugin Home sync, CPA trace ID middleware, selected-auth index metadata callbacks, Codex overlong encrypted-reasoning ID sanitize, Codex parallel tool-call normalization / input-id shortening / agent-scoped replay cache isolation, Kimi chat-completions path + Claude-executor routing + native thinking object, xAI `additional_tools` promotion / image-url rewrite / compact base URL / root-union normalization, count_tokens endpoint-not-found availability-neutral handling, Gemini 3.1 Flash Lite + Kimi K3 model catalog updates, translator single-pass array builders / file-data MIME extraction / OpenAI response namespace qualification, and Redis plugin sync timeout options.
- 2026-07-17: added consecutive status-disable for credentials (v1: xAI/Grok only). After 3 consecutive policy 403s the auth is disabled like 401 unauthorized; progress is stored in auth metadata `consecutive_status_failures` and only mutates on count change (0→1/1→2/disable/clear). Success or any non-policy result clears the streak; Cloudflare challenge 403s do not count. Policy table lives in `sdk/cliproxy/auth/consecutive_status_disable.go` for future provider/status expansion. Frontend auth-files status-code filter gains `403` alongside `401`/`429`.
- 2026-07-17: merged backend upstream `main` through `106270be` (`v7.2.81`) and frontend upstream through `6a6a22a` (`v1.18.5`). Backend conflicts in `go.mod`/`go.sum` (keep fork `modernc.org/sqlite` for `usagepersist`; adopt upstream x/crypto/net/sync versions), `sdk/cliproxy/usage/manager.go` + tests (keep fork `WithUsageSuppressed` / skip-on-publish while adopting upstream `Generate` flag helpers). Hardened `configureCooldownStateStore` to always set a nil store (upstream SaveCooldownStatus/.cds path stays merged for mergeability but must not re-enable after the nil assignment). Backend adopted upstream Kimi dynamic buildinfo version headers, Generate usage tracking defaults, gitstore client-options API + disabled commit signing, translator tool-name disambiguation/dedup and incomplete-status handling, xAI image usage reporting completion, plugin path tilde expansion, deferred request-body capture for errors, `SelectAuthByKind` / Home force-mapping, and related auth/scheduler tests. Frontend conflicts in `useProviderRecentRequests` (connection-scoped cache from upstream + fork force-refresh wait for in-flight reads so clear-cooldown never reuses pre-clear payloads) and `OAuthPage` (keep Gemini CLI card/`project_id` while adopting Kimi featured card + sign-up). Frontend also adopted Kimi provider workbench support, auth-file OAuth write guards after load failures, inline-quota isolation, and sponsor endpoint visibility fixes. Fork follow-up same session: Codex quota management reset-credit consume for non-free plans (Plus/Pro/Team with available `rate_limit_reset_credits`), gated by `canResetCodexQuota` (explicitly excludes free), with count-aware button/confirm copy aligned to CPA-Manager-Plus semantics via management `/api-call` to ChatGPT `rate-limit-reset-credits/consume` — distinct from local routing `POST /reset-quota`.
- 2026-07-14: restored fork Gemini CLI OAuth login UI that was incomplete after earlier upstream merges. Backend TUI `submitCallback` again maps `gemini-cli-auth-url` → provider key `gemini` (removed by upstream `78ba8ba7`, list entry was restored earlier without this case). Frontend management OAuth page re-adds `gemini-cli` card + project ID field, `is_webui`/`project_id` start options, callback map `gemini-cli`→`gemini`, and `auth_login.gemini_cli_*` i18n from pre-`ed4124f` history (upstream removal had been absorbed while only Gemini CLI quota UI was previously restored).
- 2026-07-14: merged backend upstream `main` through `06a4e46f` (`v7.2.73`). Conflicts in `internal/watcher/synthesizer/config.go` (keep fork `addFailureWarmupToAttrs` on the shared Codex/xAI `synthesizeCodexStyleKeys` path) and `internal/api/server_test.go` (adopt upstream Codex template parse error text). Backend adopted upstream `xai-api-key` config/management CRUD/synthesizer/conductor model-alias wiring, xAI X Search / namespaced tool routing and response filtering, Codex client model catalog remote refresh + validation, Codex tool-search model-template gating, OpenAI Responses signature sanitize performance/orphan-id fixes, README provider logos, and FreeBSD release `GOARCH` rename. Frontend had no new upstream commits beyond `d3df9b0` (`v1.18.3`). Fork follow-ups in the same session: `usagepersist` now stores/returns `ttft_ms` for first-token latency; usage page recent rows merge token columns, show cache hit %, and show `ttft/total` latency; auth-files xAI inspection probes `cli-chat-proxy.grok.com` chat endpoints (grok-inspection style) instead of billing-only health, while reuse existing filters and bulk disable/delete/archive for remediation.
- 2026-07-13: merged backend upstream `main` through `5b7f2361` (`v7.2.71`) and frontend upstream through `d3df9b0` (`v1.18.3`). Clean merges with no conflict markers. Backend adopted upstream model display-name config across APIs, xAI encrypted reasoning replay hardening (caller-auth isolation, compaction clear, tool-call-only batches), Codex alpha-search session affinity / Gin-context auth selection, GPT-5.6 standalone search proxying, OpenAI Responses namespace/custom-tool handling and streaming `call_id` synthesis, image handler `ExecuteImage(With|Stream)WithAuthManager` renames, and Cubence/FastAIToken README sponsor assets. Fork-owned surfaces verified intact after auto-merge: `usagepersist` cooldown path, hard-disabled file-backed `.cds` store in `configureCooldownStateStore`, Gemini CLI packages/translators, image fallback handlers, archived-auth / failure-warmup conductor guards. Frontend adopted `disable-image-generation: passthrough` mode and simplified Codex additional-window classification via `pickClassifiedWindows`; Gemini CLI quota section and dense persisted quota rows remain.
- 2026-07-11: merged backend upstream `main` through `e99a2056` (`v7.2.66`) and frontend upstream through `07562b7` (`v1.18.1`). Backend conflict resolution kept the fork-owned Gemini CLI TUI login entry while adopting upstream cancelable OAuth sessions and device-code flags for Kimi/xAI. Frontend quota conflicts kept dense persisted quota rows and selected refresh behavior while adopting upstream connection-scoped quota cache generation guards; the follow-up v1.18.1 sync also added upstream auth-file official-API selection support. This sync restored all Gemini CLI quota translations, made management `/api-call` apply auth-file custom headers before `$TOKEN$` substitution, fixed the auth-files filter grid/duplicate compact toggle, and serialized operator cooldown clears behind queued SQLite writes; forced provider-health refreshes must wait out pre-clear in-flight reads before fetching the post-clear state.
- 2026-07-10: merged backend upstream `main` through `26d45fd4` (`v7.2.58`). Conflict resolutions kept fork-owned archived-auth status handling in management auth-file toggles, fork GPT 5.5/static catalog coverage, failure warmup state, and unauthorized-disabled refresh recovery while preserving upstream model override headers, Codex response websocket request logging, automatic OAuth credential refresh after upstream 401 responses, Claude model ID prefix handling, cache-control translation, Gemini `max_tokens` mapping, xAI reasoning effort registry integration, Codex input modality restrictions, default client/user-agent model updates, and GPT-5.6 model registry churn through the Sol removal. Keep the upstream 401 refresh path serialized by `refreshLocks`, but continue to reject archived auth refreshes and to restore fork persisted unauthorized/disabled state only after a successful credential refresh. README sponsor assets for Fenno.ai and Qiniu Cloud are merged as upstream content.
- 2026-07-08: merged backend upstream `main` through `14b13966` (`v7.2.52`) and frontend upstream through `4064b01` (`v1.17.10`). Upstream removed Gemini CLI support globally (auth, executor, translators, thinking provider, login flows, virtual auth expansion, frontend quota grouping); this fork keeps Gemini CLI as a fork-owned feature and restored all of it during the merge, including `type: "gemini"` auth-file synthesis, multi-project virtual auth expansion, grouped scheduler/legacy-selector rotation, `/v1internal:method` and `/google/callback` routes, management `gemini-cli-auth-url` login, model registration, and the frontend Gemini CLI quota section. Cooldown persistence decision: keep the fork `internal/usagepersist` SQLite path (asynchronous incremental writes off the request path, legacy auth-index migration, model-support-aware restore, integrated with api-key-usage/clear-cooldown/quota snapshots); upstream's new file-backed `.cds` `CooldownStateStore` code is merged but hard-disabled in `configureCooldownStateStore` (sdk/cliproxy/service.go) so the two mechanisms never double-persist or double-restore — keep it disabled in future merges. Upstream sponsor/affiliate surfaces (APIKEY.FUN quick-start panel and routing, Code0/ClaudeAPI provider presets, dashboard quick-start card) are merged at the code level to reduce future conflicts but all UI entry points are removed in the frontend (no nav item, no route, no dashboard card, sponsor brands filtered out of the providers workbench group list); keep them hidden when resolving future conflicts. Also preserved fork statusCode/archive filters alongside upstream's new enabled-only auth-files filter, merged upstream provider-key compat grouping (`apiKeyUsageProviderKey`/`executorKeyFromAuth`) with fork Archived exclusions, and adopted upstream Google Interactions, OAuth model alias force-mapping, ResetQuota endpoint, plugin store/OAuth updates, and gpt-image-1.5 direct image proxying.
- 2026-06-17: merged backend upstream `main` through `644ba74b` (`v7.2.14`) and frontend upstream through `e95cc2b` (`v1.16.8`). Conflict resolutions kept fork-owned auth-file archival, AI provider cooldown visibility/clear-cooldown management behavior, Codex quota persistence from WebSocket `codex.rate_limits`, quota snapshot refresh metadata, and legacy plugin executor empty-format compatibility while preserving upstream config API-key exclusion management, pluginhost model routing/stream callbacks, xAI websocket/executor updates, model registry updates, and the upstream Amp/Ampcode removal. Frontend conflict resolution removed Ampcode UI/API surfaces, kept provider cooldown details/actions on `/api-key-usage`, and adapted the quota page to upstream Antigravity `retrieveUserQuotaSummary` group/bucket payloads plus Gemini CLI grouped quota buckets.
- 2026-06-14: merged upstream `main` through `83c53d05` (`v7.1.75`). Conflict resolutions kept fork-owned AI provider cooldown visibility/clear-cooldown management behavior, GPT 5.5/static catalog tests, and scheduler fill-first sticky nearest-reset behavior while preserving upstream plugin store latest-release/install/delete APIs, Antigravity native web search routing, plugin CORS/config fixes, translator mid-conversation system consolidation, and model registry updates. Plugin executor adapters should continue to accept legacy empty input/output format declarations by defaulting to OpenAI, but follow upstream's split input/response format contract: `RequestToFormat` resolves the executor input format, `ResponseFormat` controls downstream response translation, and plugin executor `SourceFormat` reflects the selected native input format for the executor call.
- 2026-06-08: merged upstream `main` through `c989cdd9`. Conflict resolutions kept fork-owned credential creation timestamps, auth status/cooldown persistence, Codex quota persistence from already-observed HTTP/WebSocket metadata, and quota cooldown restore after model registration while preserving upstream pluginhost, Redis queue, request logging, Cloudflare challenge retry, Codex reasoning replay cache, Home auth retry, safemode, FreeBSD release workflow, and model registry updates. Keep plugin executor adapters backward-compatible with empty format declarations by defaulting to OpenAI payload format while preserving the original `SourceFormat` metadata; this keeps upstream pluginhost panic/fuse tests green and avoids forcing legacy plugin executors to declare formats immediately.
- 2026-06-01: merged upstream `main` through `05b97247` (`v7.1.37`). Conflict resolution kept fork GPT 5.5/static model catalog tests while adding upstream xAI `grok-imagine-video-1.5-preview` coverage. Upstream introduced Codex identity obfuscation refinements, signature replay compatibility checks, Home app log forwarding, and model registry updates. Keep `setHeaderCasePreserved` updating an existing case-equivalent key instead of recreating it, so HTTP Codex requests retain `Session_id` while WebSocket requests retain lowercase `session_id`.
- 2026-05-29: merged upstream `main` through `df0176a1`. Conflict resolutions kept fork-owned auth status/cooldown visibility, Codex quota persistence from WebSocket handshakes/events, image fallback usage event publishing, and Antigravity credits hints while preserving upstream service-tier/TTFT usage tracking, websocket auth metadata, request logging, signature validation extraction, and model registry updates. Keep Antigravity credits `loadCodeAssist` defaulting to the daily endpoint unless a custom `base_url` is configured, and keep bootstrap retry auth-selection errors surfaced as `auth_unavailable` after an upstream bootstrap failure.

### Fork-Owned Backend Features
- Consecutive status-disable (general policy table, v1 xAI-only): `sdk/cliproxy/auth/consecutive_status_disable.go`. After N consecutive matching HTTP status failures on one credential, disable it via the same durable path as 401 (`Disabled`, `Status=disabled`, persisted `last_error`). v1 policy: provider `xai`, status `403`, threshold `3`. Counter is in-memory `Auth.consecutiveStatusFailures` and mirrored to auth JSON metadata key `consecutive_status_failures` (e.g. `{"403":2}`) only when the count changes; success or any non-policy outcome clears memory+metadata so restarts cannot resurrect a broken streak mid-way, and healthy traffic with count=0 does not dirty the credential file. Cloudflare challenge 403s do not increment. Operator re-enable clears the counter. Extend by adding rows to `consecutiveStatusDisablePolicies` rather than hard-coding more branches in `MarkResult`.
- `internal/usagepersist`: fork-owned SQLite persistence for usage statistics, request event history, request status codes, and auth/model cooldown state. Upstream may not contain this package; keep imports under the module path `github.com/router-for-me/CLIProxyAPI/v7/...` because the Go module path follows upstream even in this fork.
- Usage persistence integration points include server startup, management APIs, usage event building, status-code persistence for provider failures, and auth cooldown restore/migration.
- Usage detail rows expose `tokens` plus `latency_ms` and `ttft_ms`. The management usage page recent-request table shows tokens as `input / output / total`, cache hit percent as `cached_tokens / input_tokens`, and latency as `ttft / total`. Missing historical TTFT values render as empty placeholders without backfill.
- Auth-files credential inspection is a frontend, operator-triggered diagnostic over management `/api-call`, not a scheduler. It includes disabled and archived credentials and prefers provider quota/billing/identity evidence already shared with the quota page. Keep persisted lifecycle state separate from diagnostic health and keep all state-changing suggestions confirmation-gated. For xAI, distinguish explicit quota exhaustion, invalid auth, entitlement denial, policy denial, ambiguous permissions, model/endpoint mismatch, and upstream/protocol failures; never classify every 403 as invalid auth. Real inference probes may be used only where the existing provider quota implementation requires them and must not turn ambiguous results into destructive actions.
- `internal/imagesfallback` and OpenAI image fallback handlers: fork-owned image fallback path that can route failed image generation/edit requests through Codex OAuth-capable auths. Default `gpt-image-2` `/v1/images/generations` and `/v1/images/edits` requests should try routed image providers first, then fall back to Codex OAuth/ChatGPT image fallback on retryable failures when a Codex OAuth auth exists; keep the primary routed phase free-auth-disallowed so free accounts are reserved for the fallback path. ChatGPT web image fallback may use `CLI_PROXY_WEB_IMAGE_PROXY_URL` or `WEB_IMAGE_PROXY_URL` as a dedicated proxy before falling back to global `proxy-url`; per-auth `proxy-url` still has priority and may use `direct`/`none` to bypass proxies.
- Image-only 401/403 responses from the primary `/images/*` path or ChatGPT web fallback are endpoint-specific and must not disable a Codex credential or overwrite its text-auth health. Primary image 401s skip the generic automatic OAuth refresh path, remain model-scoped, and rotate to the next credential; web fallback rotation uses a text model as the auth-health selector and skips generic `MarkResult` tracking. For routed image requests, each paid/API-key Codex credential selected after a fallback failure must retry the primary `/images/*` path before its OAuth credential tries ChatGPT web fallback; a web authorization failure then rotates to the next credential and restarts from that credential's primary path. Free Codex OAuth credentials remain reserved for web fallback and skip the primary routed phase. Direct Codex image requests apply auth-file `headers.authorization` after the built-in stored access token so the custom value is sent upstream; keep an HTTP-level regression test for this precedence.
- Management/usage analytics contract: backend responses should continue to provide enough detail for the frontend usage page, including persisted events, status codes when known, credential identifiers, and cooldown summaries.
- Persisted usage events already record the concrete executor/provider. Keep `provider` in each `/v0/management/usage` detail row so the frontend can attribute requests from the selected credential instead of guessing only from model names; legacy/provider-less Grok rows should still fall back to `grok`/`xai` inference.
- AI providers page channel health uses `/v0/management/api-key-usage`; keep its response shape as `provider -> "base_url|api_key" -> success/failed/recent_requests` with additive auth status fields. The endpoint should restore counts and 20-bucket status bars from persisted `usage_events` by live `auth_index` when available, then fall back to runtime `Auth.Success`/`Auth.Failed` and in-memory recent buckets when no persisted rows exist. Provider rows may show runtime API-key cooldown or model suspension state from the same response, and `POST /v0/management/api-key-usage/clear-cooldown` is the management action for manually clearing operator-retryable API-key availability blocks, including quota/429 cooldowns and model-level 404/not_found or model_not_supported pauses after the operator fixes configuration. It must not clear disabled or 401 unauthorized credentials.
- Auth file health contract: only upstream/provider 401s should mark credentials disabled or contribute to the auth-files 401 classification. Management UI to backend 401s must never be treated as credential failures. Generic management `/api-call` 401 handling must require a substituted credential token and a trusted provider HTTPS host before disabling an auth.
- Auth file creation timestamps are persisted in auth JSON metadata under `credential_created_at`. Do not use provider payload `created_at` for credential creation time because many provider response formats already define that field. OAuth login/re-login and management upload overwrite create a new credential timestamp; token refresh and disabled-state persistence preserve the existing `credential_created_at`.
- Auth file archival is a manual operator state persisted in auth JSON metadata under `archived`. Keep it independent from `disabled`: archiving a 401-disabled credential must preserve `disabled`, `last_error`, and the auth-files `status_code` evidence, and unarchiving must not automatically re-enable or clear a 401. Archived credentials should be hidden from normal auth-files views in the management UI and skipped by routing, scheduler ready sets, auto/manual credential refresh, model registration, normal quota-page refreshes, websocket availability, image fallback, and model-fetch utilities until manually unarchived. The operator-triggered auth-files inspection is the deliberate exception: it may use management `/api-call` with the archived auth's stable `auth_index` to gather read-only provider evidence before offering an explicit unarchive/restore action.
- Codex quota visibility is persisted opportunistically from already-observed Codex HTTP rate-limit headers and WebSocket `codex.rate_limits` events via `internal/codexquota`; keep this persistence best-effort and asynchronous, and do not add quota polling or extra provider calls to normal request execution for this management view.
- Quota page token usage is per inferred quota refresh cycle when a stored quota snapshot exposes cycle/reset metadata; credentials without usable cycle metadata fall back to lifetime aggregation. Successful management quota snapshots that prove available capacity may clear stale persisted/runtime quota or 429 cooldowns, but never clear disabled credentials or HTTP 401 unauthorized auth state.
- Provider-neutral quota reset routing hints are stored in `usage.sqlite` through `quota_routing_hints` and loaded into auth scheduler memory on startup/register. Existing background credential refresh/maintenance may collect provider quota snapshots for Codex, Claude, Gemini CLI, Antigravity, Kimi, and xAI when their OAuth quota endpoints expose reset metadata. The scheduler may prefer credentials with the nearest known quota reset within the same manual priority, but request execution must remain best-effort and non-blocking: no hot-path database reads, no request-path quota polling, and no provider-specific probes from normal proxy requests. Keep nearest-reset ordering consistent for scheduler fast paths and built-in legacy selector fallbacks, and publish Codex management snapshot writes into in-memory routing hints immediately after persistence. Management snapshot writes that arrive with only `auth_index` must resolve and persist the live `auth_id` before publishing routing hints. Scheduler ready buckets must detect relevant in-memory hint changes and reorder affected shards without resetting unrelated provider/model queues. Fill-first remains sticky: nearest-reset ordering chooses the first ready credential, then the same credential should continue to be selected until it becomes unavailable, disabled, cooling down, or excluded from the current request.
- API-key provider entries may expose `failure-warmup` (`enabled`, `status-codes`, `max-attempts`) for Gemini, Claude, Codex, OpenAI-compatible, and Vertex-compatible providers. When enabled and a normal request fails with a configured upstream status, the auth manager queues an in-memory asynchronous retry using the already prepared original request. This retry must never block the client response, call `MarkResult`, publish usage/request-history records, increment usage counters, or re-run selected-auth request interceptors; a successful warmup may only clear recoverable availability/cooldown state for that auth. Deduplicate by auth ID while a warmup is in flight.
- Cooldown persistence compatibility matters. Preserve legacy auth index matching and migration behavior so existing `usage.sqlite` databases remain readable after syncs.
- Gemini CLI support is fork-owned since upstream removed it (2026-07-08 sync). Fork-owned surfaces include `internal/auth/gemini`, `internal/runtime/executor/gemini_cli_executor.go`, `internal/runtime/geminicli`, `internal/thinking/provider/geminicli`, all `gemini-cli` translator packages, `sdk/api/handlers/gemini/gemini-cli_handlers.go`, `sdk/auth/gemini.go`, `internal/cmd/login.go`, Gemini OAuth management login in `auth_files.go`/`api_tools.go`, watcher `type: "gemini"` synthesis with multi-project virtual auth expansion, scheduler/legacy-selector grouped rotation (`virtualParent`/`groupByVirtualParent`), `GeminiCLI` constants, `FormatGeminiCLI`, `GetGeminiCLIModels`, and the frontend Gemini CLI quota section. When upstream refactors shared files, restore these hooks rather than dropping them; `sdk/auth/filestore.go` and the file synthesizer must not skip `type: "gemini"` credentials.
- xAI build/CLI quota refresh uses the CLI chat proxy billing endpoints and requires the OAuth subject as `x-userid`. Keep the xAI auth-file list entry exposing only the safe `sub` identity field needed by the frontend; never expose access, refresh, or ID token values. The frontend should continue to send `Authorization: Bearer $TOKEN$` so token resolution stays inside the trusted management API.
- Upstream's file-backed cooldown store (`sdk/cliproxy/auth/cooldown_state.go`, config `save-cooldown-status`) stays merged but disabled: `configureCooldownStateStore` always sets a nil store because the fork persists cooldowns through `internal/usagepersist` (SQLite, asynchronous incremental writes, legacy-index migration). Do not enable both mechanisms at once.

### Frontend Coupling Notes
- The management UI is a separate React/Vite project at `/Users/yuanwan/Cli-Proxy-API-Management-Center`.
- Its upstream repository is `router-for-me/Cli-Proxy-API-Management-Center`; the local fork remote is `yuanwangz/Cli-Proxy-API-Management-Center`.
- Backend management API changes that affect usage analytics, credential health, cooldown counts, auth file metadata, or release asset loading must be coordinated with the frontend project.
- Gemini CLI OAuth login on the management `/oauth` page is fork-owned (upstream removed it in `ed4124f`). Keep the built-in `gemini-cli` provider card, optional `project_id` / `ALL` start options, `is_webui` auth URL start, callback provider mapping `gemini-cli`→`gemini`, and `auth_login.gemini_cli_*` i18n strings. Backend TUI OAuth must map `gemini-cli-auth-url` to callback provider `gemini`.
- Auth file list responses expose `created_at` as the credential creation time sourced from `credential_created_at`; the management UI should display it without adding wide table columns that break dense credential lists.
- Auth file list responses expose `archived` and `status: "archived"` for sealed credentials. The management UI defaults to showing unarchived credentials, provides archived/all filters, and uses `PATCH /v0/management/auth-files/status` with `{ name, archived }` for manual archive/unarchive actions.
- Auth-files status-code filters include `401`, `403`, and `429`. Credentials disabled after consecutive policy 403s expose `status_code: 403` from persisted `last_error` so the 403 filter matches them.
- Auth-files credential inspection results have a frontend-only filter layer on the fork's dense list page. The `healthy` filter drives pagination and filtered selection, while inspection results remain transient UI diagnostics and are not persisted as credential lifecycle state.
- Quota page availability filters may use a successful stored/refreshed quota snapshot to override stale quota-limited auth-file state, but disabled and 401 unauthorized credentials must remain unavailable until re-enabled or reauthorized.
- Usage detail rows from `/v0/management/usage` may include `error` with the captured upstream failure response body/message, truncated by the backend to keep persisted events bounded. The management usage page renders failed rows as a clickable error label with a detail popover; old events without captured failure bodies should keep a graceful fallback.
- Provider edit forms should preserve and edit `failure-warmup` using backend field names `failure-warmup.enabled`, `failure-warmup.status-codes`, and `failure-warmup.max-attempts`. Defaults shown to operators are enabled=false, selected statuses `[429, 500, 502, 503, 504, 529]`, and max attempts `5`; suggested selectable statuses also include `403, 408, 409, 423`.
- Codex quota management supports ChatGPT rate-limit reset credits for non-free plans: after a successful usage refresh, the UI may show a "重置额度" action when `canResetCodexQuota` is true (status success, planType not free, available reset credits > 0). Consume goes through management `/api-call` POST to `https://chatgpt.com/backend-api/wham/rate-limit-reset-credits/consume` with `redeem_request_id`, then re-fetches usage/reset-credit details. This is not the local scheduler/routing `POST /v0/management/reset-quota` endpoint. Free-tier cards must not expose a working reset action.
- The frontend release workflow is tag-driven: pushing a `v*` tag builds and publishes `dist/management.html`. If the user asks for a same-version frontend release, do not change `package.json` version; update the release/tag flow deliberately and verify the workflow result.

## Commands
```bash
gofmt -w . # Format (required after Go changes)
go build -o cli-proxy-api ./cmd/server # Build
go run ./cmd/server # Run dev server
go test ./... # Run all tests
go test -v -run TestName ./path/to/pkg # Run single test
go build -o test-output ./cmd/server && rm test-output # Verify compile (REQUIRED after changes)
```
- Common flags: `--config <path>`, `--tui`, `--standalone`, `--local-model`, `--no-browser`, `--oauth-callback-port <port>`

## Config
- Default config: `config.yaml` (template: `config.example.yaml`)
- `.env` is auto-loaded from the working directory
- Auth material defaults under `auths/`
- Storage backends: file-based default; optional Postgres/git/object store (`PGSTORE_*`, `GITSTORE_*`, `OBJECTSTORE_*`)

## Architecture
- `cmd/server/` — Server entrypoint
- `internal/api/` — Gin HTTP API (routes, middleware, modules)
- `internal/thinking/` — Main thinking/reasoning pipeline. `ApplyThinking()` (apply.go) parses suffixes (`suffix.go`, suffix overrides body), normalizes config to canonical `ThinkingConfig` (`types.go`), normalizes and validates centrally (`validate.go`/`convert.go`), then applies provider-specific output via `ProviderApplier`. Do not break this "canonical representation → per-provider translation" architecture.
- `internal/runtime/executor/` — Per-provider runtime executors (incl. Codex WebSocket)
- `internal/translator/` — Provider protocol translators (and shared `common`)
- `internal/registry/` — Model registry + remote updater (`StartModelsUpdater`); `--local-model` disables remote updates
- `internal/store/` — Storage implementations and secret resolution
- `internal/managementasset/` — Config snapshots and management assets
- `internal/cache/` — Request signature caching
- `internal/watcher/` — Config hot-reload and watchers
- `internal/wsrelay/` — WebSocket relay sessions
- `internal/usage/` — Usage and token accounting
- `internal/tui/` — Bubbletea terminal UI (`--tui`, `--standalone`)
- `sdk/cliproxy/` — Embeddable SDK entry (service/builder/watchers/pipeline)
- `test/` — Cross-module integration tests

## Code Conventions
- Keep changes small and simple (KISS)
- Comments in English only
- If editing code that already contains non-English comments, translate them to English (don’t add new non-English comments)
- For user-visible strings, keep the existing language used in that file/area
- New Markdown docs should be in English unless the file is explicitly language-specific (e.g. `README_CN.md`)
- As a rule, do not make standalone changes to `internal/translator/`. You may modify it only as part of broader changes elsewhere.
- If a task requires changing only `internal/translator/`, run `gh repo view --json viewerPermission -q .viewerPermission` to confirm you have `WRITE`, `MAINTAIN`, or `ADMIN`. If you do, you may proceed; otherwise, file a GitHub issue including the goal, rationale, and the intended implementation code, then stop further work.
- `internal/runtime/executor/` should contain executors and their unit tests only. Place any helper/supporting files under `internal/runtime/executor/helps/`.
- Follow `gofmt`; keep imports goimports-style; wrap errors with context where helpful
- Do not use `log.Fatal`/`log.Fatalf` (terminates the process); prefer returning errors and logging via logrus
- Shadowed variables: use method suffix (`errStart := server.Start()`)
- Wrap defer errors: `defer func() { if err := f.Close(); err != nil { log.Errorf(...) } }()`
- Use logrus structured logging; avoid leaking secrets/tokens in logs
- Avoid panics in HTTP handlers; prefer logged errors and meaningful HTTP status codes
- Timeouts are allowed only during credential acquisition; after an upstream connection is established, do not set timeouts for any subsequent network behavior. Intentional exceptions that must remain allowed are the Codex websocket liveness deadlines in `internal/runtime/executor/codex_websockets_executor.go`, the wsrelay session deadlines in `internal/wsrelay/session.go`, the management APICall timeout in `internal/api/handlers/management/api_tools.go`, and the `cmd/fetch_antigravity_models` utility timeouts
